<div class="container-fluid">
  <div class="row">
    <div class="col-md-6">
    	<form role="form-horizontal" role="form">
          <div class="row form-group">
              <label class="col-xs-9">Title number of less than 4 minutes </label>
              {{input type="number" class="col-xs-3 count-input first" value=firstSliceCount min="0" max="30"}}
          </div>
          <div class="row form-group">
            <label class="col-xs-9">Title number between 4 and 8 minutes </label>
            {{input type="number" class="col-xs-3 count-input second" value=secondSliceCount min="0" max="30"}}
          </div>
          <div class="row form-group">
            <label class="col-xs-9">Title number between 8 and 12 minutes </label>
            {{input type="number" class="col-xs-3 count-input third" value=thirdSliceCount min="0" max="30"}}
          </div>
          <div class="row form-group">
            <label class="col-xs-9">Title number longer than 12 minutes </label>
            {{input type="number" class="col-xs-3 count-input fourth" value=fourthSliceCount min="0" max="30"}}
          </div>
        </form>
    </div>    
    <div class="col-md-6">
    	<table class="table table-striped">
        <tr>
        <td><span id="total-count">{{totalCount}}</span> x mastering (individual tuning for each title)</td>
        <td id="mastering-price">{{titlesPrice}}</td>
        </tr>
      </table>
    </div>
  </div>
</div>